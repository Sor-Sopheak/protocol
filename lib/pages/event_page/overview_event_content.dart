import 'package:flutter/material.dart';

class OverviewEventContent extends StatelessWidget {
  const OverviewEventContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}